# Codex Instructions for PositionManager.jl

This repository contains the Julia package `PositionManager.jl`.

Before editing:

1. Read `../AGENTS.md`.
2. Read `docs/ai/implemented-surface.md`.
3. Read `docs/ai/instructions/architecture.instructions.md`.
4. Read any additional package-local instruction file only when it is clearly task-relevant.

Use `docs/ai/_archive/` only for historical context.
